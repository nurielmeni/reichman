var JobSearch =
    JobSearch ||
    (function ($) {
        "use strict";

        function init() {
            console.log('Job search Init');
        }

        return {
            init: init
        }
    })(jQuery);

JobSearch.init();
